#include <iostream>
#include <vector>
int main(void)
{
    int t,n;
    std::cin>>t;
    for(int i=0;i<t;i++)
    {
        int time=0;
        std::cin>>n;
        std::vector<int> x1(n);
        std::vector<int> x2(n);
        std::vector<int> y1(n);
        std::vector<int> y2(n);
        for(int j=0;j<n;j++)
        std::cin>>x1[j]>>y1[j]>>x2[j]>>y2[j];


    }


}
